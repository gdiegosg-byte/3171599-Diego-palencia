# ============================================
# CD WORKFLOW - Proyecto Final
# Construye y publica imagen Docker en GHCR
# ============================================

name: CD

on:
  push:
    branches: [main]
    tags: ['v*.*.*']

env:
  REGISTRY: ghcr.io
  # TODO: Cambiar por tu usuario/repo
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # ============================================
  # JOB: Build and Push
  # Construye imagen Docker y la publica en GHCR
  # ============================================
  build-and-push:
    name: 🐳 Build & Push
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      # TODO: Login a GitHub Container Registry
      # - name: Login to GHCR
      #   uses: docker/login-action@v3
      #   with:
      #     registry: ${{ env.REGISTRY }}
      #     username: ${{ github.actor }}
      #     password: ${{ secrets.GITHUB_TOKEN }}

      # TODO: Extraer metadata para tags
      # - name: Extract metadata
      #   id: meta
      #   uses: docker/metadata-action@v5
      #   with:
      #     images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
      #     tags: |
      #       type=ref,event=branch
      #       type=semver,pattern={{version}}
      #       type=semver,pattern={{major}}.{{minor}}
      #       type=sha,prefix=

      # TODO: Construir y publicar imagen
      # - name: Build and push
      #   uses: docker/build-push-action@v5
      #   with:
      #     context: .
      #     push: true
      #     tags: ${{ steps.meta.outputs.tags }}
      #     labels: ${{ steps.meta.outputs.labels }}
