# ============================================
# CI WORKFLOW - Proyecto Final
# Ejecuta lint, tests y build en cada push/PR
# ============================================

name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  # ============================================
  # JOB: Lint
  # Verifica código con ESLint
  # ============================================
  lint:
    name: 🔍 Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 8

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Instalar dependencias
        run: pnpm install --frozen-lockfile

      # TODO: Ejecutar ESLint
      # - name: Ejecutar ESLint
      #   run: pnpm lint

  # ============================================
  # JOB: Test
  # Ejecuta tests con Vitest
  # ============================================
  test:
    name: 🧪 Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 8

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Instalar dependencias
        run: pnpm install --frozen-lockfile

      # TODO: Ejecutar tests con cobertura
      # - name: Ejecutar tests
      #   run: pnpm test:coverage

      # TODO: Subir reporte de cobertura (opcional)
      # - name: Upload coverage
      #   uses: codecov/codecov-action@v3
      #   with:
      #     file: ./coverage/lcov.info

  # ============================================
  # JOB: Build
  # Construye la aplicación
  # ============================================
  build:
    name: 🏗️ Build
    runs-on: ubuntu-latest
    needs: [lint, test]

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 8

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Instalar dependencias
        run: pnpm install --frozen-lockfile

      # TODO: Construir aplicación
      # - name: Build
      #   run: pnpm build

      # TODO: Subir artefactos (opcional)
      # - name: Upload build artifacts
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: dist
      #     path: dist/
      #     retention-days: 7
