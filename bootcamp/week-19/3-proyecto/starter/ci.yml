# ============================================
# CI WORKFLOW - PROYECTO SEMANAL
# TODO: Completar este archivo
# ============================================

name: CI Pipeline

# TODO: Configurar triggers
# on:
#   push:
#     branches: [main, develop]
#   pull_request:
#     branches: [main]

# TODO: Agregar variable de entorno para versión de Node
# env:
#   NODE_VERSION: '20'

jobs:
  # ═══════════════════════════════════════════
  # TODO: JOB DE LINTING
  # ═══════════════════════════════════════════
  # lint:
  #   name: 🔍 Lint
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: pnpm/action-setup@v3
  #       with:
  #         version: 9
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: ${{ env.NODE_VERSION }}
  #         cache: 'pnpm'
  #     - run: pnpm install --frozen-lockfile
  #     - run: pnpm lint

  # ═══════════════════════════════════════════
  # TODO: JOB DE TESTS
  # ═══════════════════════════════════════════
  # test:
  #   name: 🧪 Test
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: pnpm/action-setup@v3
  #       with:
  #         version: 9
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: ${{ env.NODE_VERSION }}
  #         cache: 'pnpm'
  #     - run: pnpm install --frozen-lockfile
  #     - run: pnpm test:coverage
  #     - uses: actions/upload-artifact@v4
  #       with:
  #         name: coverage
  #         path: coverage/

  # ═══════════════════════════════════════════
  # TODO: JOB DE BUILD
  # Debe depender de lint y test
  # ═══════════════════════════════════════════
  # build:
  #   name: 🏗️ Build
  #   runs-on: ubuntu-latest
  #   needs: [lint, test]
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: pnpm/action-setup@v3
  #       with:
  #         version: 9
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: ${{ env.NODE_VERSION }}
  #         cache: 'pnpm'
  #     - run: pnpm install --frozen-lockfile
  #     - run: pnpm build
  #     - uses: actions/upload-artifact@v4
  #       with:
  #         name: dist
  #         path: dist/
